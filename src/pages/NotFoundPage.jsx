import React from 'react';

const NotFoundPage = () => {
  return (
    <div>
      Sorry, the page you're looking for doesn't exist. Please double-check the
      URL or try refreshing the page.
    </div>
  );
};

export default NotFoundPage;
